{
  "builds": [
    {
      "src": "pubspec.yaml",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build/web"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "installCommand": "if [ -d 'build/web' ]; then rm -r build/web; fi && flutter pub get && flutter build web --release --no-tree-shake-icons"
}